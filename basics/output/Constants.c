#include "stdio.h"

int main(){

    //constant = fixed value that cannot be altered by the programm during execution
    //constants also always in caps (readability)
    const float PI = 3.141592;

    //PI = 420.69;

    printf("%f", PI);

    return 0;
}