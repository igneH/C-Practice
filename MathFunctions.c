#include "stdio.h"
#include "math.h"

int main(){

    float A = sqrt(9);
    float B = pow(4, 2); //4²
    int C = round(3.14);
    int D = ceil(3.14); //round up
    int E = floor(3.99); //round down
    float F = fabs(-100); //how far a number is away from 0
    float G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    return 0;
}